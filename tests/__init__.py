"""Test support package for maintkit.

Present so the characterisation harness can share dataset builders and the case
registry via ``from tests import _datasets`` / ``from tests._cases import ...``.
"""
